# Bot: Juancito Ads (bot oficial / demo de la casa).
# Plantilla: `juancitoads init` la copia y le estampa los nombres del bot nuevo.
# Configurado por el skill /configurar-mi-chatbot.
# NO subir valores específicos del miembro (secrets) a git.

name = "juancitoads-bot"
main = "src/index.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[ai]
binding = "AI"

# Archivos estáticos del panel (logo, favicons). Se sirven desde la raíz:
# public/logo.png -> /logo.png. Ver public/README.md.
#
# Cloudflare atiende primero los archivos que existen en public/ y todo lo
# demás cae al Worker. Hoy no hay choque posible: las rutas del bot son
# /health, /webhooks/*, /admin/*, /api/* y /kb/*, y ninguna coincide con un
# archivo de public/. Si algún día se agrega un archivo cuyo nombre choque con
# una ruta, el archivo gana — por eso public/ NO debe tener un index.html.
[assets]
directory = "./public"

[[durable_objects.bindings]]
name = "AGENT"
class_name = "SupportAgent"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["SupportAgent"]

[[d1_databases]]
binding = "DB"
database_name = "juancitoads-bot-db"
database_id = "13acf414-7f62-4723-bd20-64612442dc07"

[[vectorize]]
binding = "KB"
index_name = "juancitoads-bot-kb"

# Bucket R2 para el catálogo. Desactivado: hoy `src/tools/catalogQuery.ts` lee el
# catálogo de D1 (tabla `catalog_items`), así que nada accede a `env.CATALOG` y
# el binding solo obligaría a habilitar R2 en la cuenta. Para reactivarlo:
# habilita R2 en el dashboard, crea el bucket y descomenta este bloque (y quita
# el `?` de CATALOG en `src/env.ts`).
# [[r2_buckets]]
# binding = "CATALOG"
# bucket_name = "juancitoads-bot-catalog"

# El puente de WhatsApp por QR. Por SERVICE BINDING y no por URL pública:
# Cloudflare rechaza con error 1042 que un Worker llame a otro de la MISMA
# cuenta por su URL — la petición ni siquiera llega, y el síntoma es un 404 que
# se lee como "la ruta no existe".
#
# DESCOMENTAR DESPUÉS DE DESPLEGAR EL PUENTE, no antes. Cloudflare rechaza el
# deploy de un Worker que declara un binding hacia otro que todavía no existe,
# y aquí mergear a `main` ES desplegar: con estas tres líneas activas antes de
# tiempo, el deploy del bot se cae y de paso no corre el reindexado de la base
# de conocimiento.
#
# El orden es: 1) correr `puente-wa.yml` (crea `juancitoads-bot-wa`),
#              2) descomentar esto y mergear.
#
# Mientras siga comentado, el canal no funciona aunque el panel muestre la
# tarjeta: sin el binding, el bot cae a la URL pública y eso da justo el 1042
# de arriba. Es un estado de paso, no una configuración válida.
# [[services]]
# binding = "PUENTE_WA"
# service = "juancitoads-bot-wa"

[vars]
BOT_NAME = "Baby Caleb"
BUSINESS_NAME = "Baby Caleb"
BOT_LANGUAGE = "es-419"
# Zona horaria del negocio (IANA). La agenda de Cal.com la usa si no hay
# CALCOM_TIMEZONE. La estampa el instalador según la región elegida.
BOT_TIMEZONE = "America/Panama"
BOT_TIER = "pro"
BUFFER_SECONDS = "15"
DASHBOARD_BASE_URL = "https://juancitoads-bot.juanarrietabusiness.workers.dev"
# WhatsApp por QR (canal alterno). El bot no habla con WhatsApp: habla con este
# puente, que sostiene el socket desde un contenedor. Ver puente-wa/.
WA_PUENTE_URL = "https://juancitoads-bot-wa.juanarrietabusiness.workers.dev"

# Secrets (set with `wrangler secret put <NAME>` — never commit values):
#   ANTHROPIC_API_KEY          required — Claude API
#   DASHBOARD_PASSWORD         required (Pro) — HTTP Basic Auth for /admin (user: admin)
#   KB_REINDEX_TOKEN           required — guards POST /kb/reindex (header: X-Reindex-Token)
#                              set with: wrangler secret put KB_REINDEX_TOKEN
#   TELEGRAM_BOT_TOKEN         per-channel — Telegram bot
#   MANYCHAT_API_KEY           per-channel — ManyChat (Pro)
#   TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN / TWILIO_WA_FROM   per-channel — Twilio WhatsApp (Pro)
#   WA_TOKEN                   per-channel — WhatsApp por QR. El MISMO valor que el
#                              secret WA_TOKEN del puente `juancitoads-bot-wa`. Solo
#                              ASCII: un token con tilde o eñe pasa toda prueba hecha
#                              por navegador y falla solo en la máquina.
#   CALCOM_API_KEY             optional — scheduleAppointment (Pro)
#   GOOGLE_SERVICE_ACCOUNT_JSON  optional — Sheets export (Pro)
#   RESEND_API_KEY             optional — handoff email
#   OWNER_EMAIL                handoff email recipient
#   OWNER_TELEGRAM_CHAT_ID     handoff notification (default channel — owner /start's the bot)
#   OWNER_WA_NUMBER            handoff WhatsApp DM recipient (Pro)
#   TWILIO_HANDOFF_CONTENT_SID approved WhatsApp template SID for the handoff DM (Pro)
#   CONTROL_PLANE_TOKEN        optional (secret) — Bearer the hosted control plane presents to call /api/*
#                              set with: wrangler secret put CONTROL_PLANE_TOKEN
#   CONTROL_PLANE_URL          optional (var) — base URL of the control plane (future reports / license check)

[triggers]
crons = ["0 3 * * *"]  # daily 3am UTC: purge old messages

# DASHBOARD_PASSWORD  (secret) — HTTP Basic Auth password for the admin dashboard; username is always "admin".
# Set it with: wrangler secret put DASHBOARD_PASSWORD
